{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "echo 'Using NIXPACKS builder'"
  },
  "deploy": {
    "healthcheckPath": "/api/health",
    "startCommand": "python3 -m flask --app backend.src.main run --host=0.0.0.0 --port=$PORT"
  }
} 