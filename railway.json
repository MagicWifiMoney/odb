{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "cd backend && python -m flask --app src.main run --host=0.0.0.0 --port=$PORT",
    "healthcheckPath": "/api/health"
  }
} 